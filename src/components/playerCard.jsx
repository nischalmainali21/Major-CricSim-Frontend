import React from "react";

const PlayerCard = ({ playerStats, playerName }) => {
  // console.log(player);
  return (
    <div>
      <h1>{playerName}</h1>
    </div>
  );
};

export default PlayerCard;
